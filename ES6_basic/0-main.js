// Import the functions from the module you created
import { taskFirst, taskNext } from './0-constants.js';

// Log the results to the console to see the output
console.log(`${taskFirst()} ${taskNext()}`);
